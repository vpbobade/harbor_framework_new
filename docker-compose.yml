version: "3"

services:

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: appdb

  app:
    build: .
    depends_on:
      - db
    ports:
      - "8000:8000"
